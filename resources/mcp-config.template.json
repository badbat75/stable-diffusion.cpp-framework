{
  "_comment": "Generated from resources/mcp-config.template.json by config-server.ps1. Load this into any MCP-aware agent (Claude Code: --mcp-config <path>, or merge into ~/.claude.json). The mcp-server.ps1 entry point reads sd-server's host/port from %LOCALAPPDATA%\\stable-diffusion.cpp\\config\\server.ini at startup, so no host/port needs to be hardcoded here.",
  "mcpServers": {
    "stable-diffusion-cpp": {
      "command": "pwsh.exe",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy", "Bypass",
        "-File", "@SCRIPT_DIR@/mcp-server.ps1"
      ]
    }
  }
}
